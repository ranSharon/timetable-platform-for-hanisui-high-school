/* const initialState = {
    grade: '',
    numOfClasses: 0,
    grades: []
};

const reducer = (state = initialState, action) => {
    if(action.type === )
    return state;
};

export default reducer; */